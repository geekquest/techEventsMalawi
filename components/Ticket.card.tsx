import React from "react";
import { Box } from "./ui/box";
import { Text } from "react-native";

const TicketCard = () => {
  return (
    <Box>
      <Text>Ticket Card</Text>
    </Box>
  );
};

export default TicketCard;
